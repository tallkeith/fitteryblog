<div class="container-fluid">
  <div class="row inner-blog-content">
    
      <% content_for :body_content_title, @category.title %>


      <% content_for :body do %>
      <h1>You are in the <%= @category.title.upcase%> Category</h1>
        <% if @posts.any? %>
          <section id="blog_posts">
            <%= render :partial => "/refinery/blog/shared/post", :collection => @posts %>
            <%= will_paginate @posts %>
          </section>
        <% else %>
          <p>
            <%= t('.no_posts') %>
          </p>
        <% end %>
      <% end %>

      <div class="col-sm-8">
      <%= render "/refinery/content_page" %>
    </div>
      <div class="col-sm-4">
        <%= render "/refinery/blog/shared/newsletter" %>
        <%= render "/refinery/blog/shared/posts" %>
        <%= render "/refinery/blog/shared/categories" %>
        <%= render "/refinery/blog/shared/follow_us" %>
      </div>
      <% content_for :stylesheets, stylesheet_link_tag('refinery/blog/frontend') %>
    
  </div>
</div>